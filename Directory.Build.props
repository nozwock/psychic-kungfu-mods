<Project>
  <!-- Define GamePath property in Local.props -->
  <Import Project="Local.props" Condition="Exists('Local.props')" />
  <PropertyGroup Condition="'$(GamePath)' != ''">
    <DllDir>$(GamePath)/JinGu_Data/Managed/</DllDir>
    <!-- gamelibs/ is from BepInExPack_JinGu -->
    <LibDir>$(GamePath)/BepInEx/gamelibs/</LibDir>

    <PluginDir>$(GamePath)/BepInEx/plugins/</PluginDir>
  </PropertyGroup>

  <PropertyGroup>
    <TargetFramework>net472</TargetFramework>
    <LangVersion>14</LangVersion>
    <Nullable>enable</Nullable>
    <!-- Ignore warning about missing XML comment on accessible types and params -->
    <NoWarn>$(NoWarn);CS1591;CS1573</NoWarn>
    <!-- Allow access of private members at runtime. -->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <!-- Enable ContinuousIntegrationBuild to shorten and anonymizes paths in debug symbols relative to the project
    directory. Breakpoints will not work, however. PathMap doesn't work for linked Common sources.
    -->
    <Deterministic>true</Deterministic>

    <RestoreAdditionalProjectSources>
      https://api.nuget.org/v3/index.json;
      https://nuget.bepinex.dev/v3/index.json;
      https://nuget.samboy.dev/v3/index.json
    </RestoreAdditionalProjectSources>
  </PropertyGroup>

  <!-- Shared source for mods
  https://github.com/dotnet/sdk/issues/2511#issuecomment-1374125096 -->
  <ItemGroup>
    <Compile Include="$(MSBuildThisFileDirectory)/Common/**/*.cs" LinkBase="Common" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="BepInEx.Analyzers" Version="1.0.8" PrivateAssets="all" />
    <PackageReference Include="BepInEx.Core" Version="5.4.21" PrivateAssets="all" />
    <!-- AutoPlugin 2.1.0 Requires SDK 10+. Use 2.0.1 if that's not ideal. -->
    <PackageReference Include="Hamunii.BepInEx.AutoPlugin" Version="2.1.0" PrivateAssets="all" />

    <!-- Allows using newer Language features in projects targeting older runtimes.
     This doesn't add any support for features that need types on runtime such as KeyValuePair deconstructing, etc. -->
    <PackageReference Include="PolySharp" Version="1.16.0">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>

    <!-- Format on build -->
    <PackageReference Include="CSharpier.MsBuild" Version="1.2.6">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers</IncludeAssets>
    </PackageReference>

    <PackageReference
      Include="BepInEx.AssemblyPublicizer.MSBuild"
      Version="0.4.2"
      PrivateAssets="all"
    />

    <Reference Include="$(DllDir)/Assembly-CSharp.dll" Publicize="true" Private="false" />
    <!-- Reference Unity assemblies and others. BaGet's UnityEngine.Module should not be referenced as those assemblies
    are not for the Chinese Unity version. -->
    <Reference
      Include="$(DllDir)/*.dll"
      Exclude="$(DllDir)/Assembly-CSharp*.dll;$(DllDir)/System*.dll;$(DllDir)/mscorlib.dll;$(DllDir)/netstandard.dll"
      Private="false"
    />
    <Reference
      Include="$(LibDir)/*.dll"
      Exclude="$(LibDir)/System*.dll;$(LibDir)/mscorlib.dll;$(LibDir)/netstandard.dll"
      Private="false"
    />
  </ItemGroup>

  <ItemGroup Condition="'$(TargetFramework.TrimEnd(`0123456789`))' == 'net'">
    <PackageReference
      Include="Microsoft.NETFramework.ReferenceAssemblies"
      Version="1.0.2"
      PrivateAssets="all"
    />
  </ItemGroup>

  <Target Name="CleanupPublishDir" AfterTargets="Publish">
    <ItemGroup Condition="'$(Configuration)' == 'Debug'">
      <FilesToDelete Include="$(PublishDir)*.deps.json;" />
    </ItemGroup>
    <!-- .pdb is not excluded, so make sure ContinuousIntegrationBuild is set when building for distribution. -->
    <ItemGroup Condition="'$(Configuration)' == 'Release'">
      <FilesToDelete Include="$(PublishDir)*.deps.json;" />
    </ItemGroup>
    <Delete Files="@(FilesToDelete)">
      <Output TaskParameter="DeletedFiles" ItemName="FilesDeleted" />
    </Delete>
  </Target>

  <Target
    Name="CopyMod"
    AfterTargets="Publish"
    Condition="'$(PluginDir)' != '' AND '$(SkipCopyMod)' != 'true'"
  >
    <!-- .NET 9+ Message will only show up with -v:d or -tl:off in terminals -->
    <Message Text="Copying mod to &quot;$(PluginDir)&quot;" Importance="High" />

    <!-- https://github.com/dotnet/msbuild/issues/5881#issuecomment-1537643217 -->
    <ItemGroup>
      <FilesToCopy Include="$([MSBuild]::EnsureTrailingSlash('$(PublishDir)'))**\*.*" />
    </ItemGroup>
    <Copy
      SourceFiles="@(FilesToCopy)"
      DestinationFolder="$([MSBuild]::EnsureTrailingSlash('$(PluginDir)/$(AssemblyName)'))%(RecursiveDir)"
    />
  </Target>
</Project>
